name: elaichi
description: Campus guide of NIT Rourkela

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #state management
  stacked: ^1.6.0

  #navigation
  auto_route: ^0.5.0
  stacked_services: ^0.4.4+3

  #inversion of control(dependency injection)
  get_it: ^4.0.2
  injectable: ^0.4.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  effective_dart: ^1.2.3
  lint: ^1.2.0

  build_runner:
  auto_route_generator: ^0.5.0
  injectable_generator: ^0.4.0+1

flutter:
  uses-material-design: true
