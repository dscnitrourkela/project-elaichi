import 'package:injectable/injectable.dart';

/// Describes the fake api environment annotation
const fake = Environment('fake');

/// Describes the real api environemnet annotation.
const real = Environment('real');
