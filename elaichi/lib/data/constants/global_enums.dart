// ignore_for_file: constant_identifier_names

enum WebMailState {
  authenticated,
  unAuthenticated,
}

enum GenderType { MALE, FEMALE, OTHERS }

enum OrgSubType { CULTURAL, FMS, HACKATHON, LITERARY, SPORTS, TECHNICAL }

enum OrgType { BRANCH, BRANCH_SEM, CLUB, FEST, HOSTEL, INSTITUTE, MESS }

enum RepeatType {
  FRIDAY,
  MONDAY,
  SATURDAY,
  SUNDAY,
  THURSDAY,
  TUESDAY,
  WEDNESDAY
}

enum StatusType { ACTIVE, DRAFT, EXPIRED }

enum TransactionType { EVENT, MERCH, REGISTRATION }
